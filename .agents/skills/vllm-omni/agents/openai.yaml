interface:
  display_name: "Vllm Omni"
  short_description: "Repo-specific patterns and workflows for vllm-omni"
  default_prompt: "Use the vllm-omni repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true